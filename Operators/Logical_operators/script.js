// AND (&&) and means condition 
let age = 25;
console.log(age > 18 && age < 30); // true (both condition ar true)

// OR (||)
let isAdmin = false;
let isEditor = true;
let isTeacher = true;
console.log(isAdmin || isEditor); // true (one condition is true)
